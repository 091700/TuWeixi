# 服务器配置
server.port=8083

# 数据库配置
spring.sql.init.mode=never
spring.datasource.url=jdbc:mysql://localhost:3306/book_platform?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=091700xixi

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Elasticsearch配置
spring.elasticsearch.uris=http://localhost:9200
sspring.elasticsearch.rest.connection-timeout=10000
spring.elasticsearch.rest.read-timeout=10000


# 文件上传配置
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# 上传文件存储路径
upload.path=uploads/

# Thymeleaf配置
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# 安全配置 (临时关闭CSRF以便测试)
spring.security.filter.order=10

spring.web.resources.static-locations=classpath:/static/,file:${upload.path}
